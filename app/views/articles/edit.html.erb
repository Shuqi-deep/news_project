<div class="flex flex-col justify-center px-10">
    <h1 class="font-bold text-2xl text-center">Edit Article</h1>
    <%= render 'articles/form', article: @article %>
    <%= link_to 'Back', article_path(@article), class: '-mt-12 p-2 text-blue-500 hover:underline transition-all' %>
</div>
